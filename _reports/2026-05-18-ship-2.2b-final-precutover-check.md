---
title: Ship 2.2b — final pre-cutover check
venture: tidy-tails
date: 2026-05-18
status: READY — preflight green, cutover backup verified; awaiting Russell's final-approval phrase
type: precutover-check
ship: 2.2b
production-ref: pgkwovokciaqnbhpttba
parent-docs:
  - _reports/2026-05-18-ship-2.2b-production-cutover-runbook.md
  - _reports/2026-05-18-ship-2.2b-cutover-operator-card.md
  - _reports/2026-05-18-ship-2.2b-production-forward-migration.sql
  - _reports/2026-05-18-ship-2.2b-production-rollback-migration.sql
---

# Ship 2.2b — final pre-cutover check

The final readiness sequence for the Tidy Tails Ship 2.2b RLS cutover, run
overnight 2026-05-18. It records what was verified, the cutover backup that was
taken, and the exact command package — and it stops at the production SQL gate.
**Production `pgkwovokciaqnbhpttba` has not been mutated.**

## 0. Bottom line

| Workstream | State |
|---|---|
| A — final production preflight (read-only) | ✅ complete — **no drift** |
| B — final cutover backup + readback | ✅ complete — **service_role, readback-clean** |
| C — cutover command package | ✅ ready (this doc §3) — not executed |
| D — Sam day-one readiness checklist | ✅ ready (this doc §4) |

**The only remaining step is Russell typing the exact phrase:**

> **`FINAL APPROVAL: run the Ship 2.2b production RLS cutover now`**

Nothing in this run applied production SQL, mutated production data, enabled a
write surface, or sent an SMS.

---

## 1. Workstream A — final production preflight (read-only)

All checks done 2026-05-18. Items 1–7 are artifact/config verifications; items
8–9 are the two read-only production SELECTs.

| # | Check | Result |
|---|---|---|
| 1 | Forward SQL carries Sam's real UID `88413167-0799-49a7-ba4c-c1c29403e038` (6 backfill UPDATEs + header), zero rehearsal-UID occurrences | ✅ |
| 2 | Forward SQL ends with `NOTIFY pgrst, 'reload schema';` | ✅ |
| 3 | Rollback SQL restores the permissive policies (20 `CREATE POLICY`) and recreates `client_overview` (1 `CREATE VIEW`) | ✅ |
| 4 | No `TIDYTAILS_ENABLE_*` write flag set in any Vercel environment | ✅ all 4 OFF |
| 5 | v2 production URL `https://tidy-tails-v2.vercel.app` responds | ✅ |
| 6 | v2 points at `NEXT_PUBLIC_SUPABASE_URL = https://pgkwovokciaqnbhpttba.supabase.co` | ✅ |
| 7 | Forward + rollback SQL both target ref `pgkwovokciaqnbhpttba` | ✅ |
| 8 | Sam's `auth.users` account exists; UID matches | ✅ `88413167-0799-49a7-ba4c-c1c29403e038` for `sammclennan143@gmail.com` |
| 9 | Production row counts (read-only `SELECT count(*)`) | ✅ see below — **no drift** |

**Item 9 — production counts, 2026-05-18 (read-only):**

| Table | Count | Expected | Drift |
|---|---|---|---|
| `clients` | 137 | 137 | none |
| `pets` | 188 | 188 | none |
| `appointments` | 737 | 737 | none |
| `booking_requests` | 0 | 0 | none |
| `client_accounts` | 0 | 0 | none |
| `automations_log` | 0 | 0 | none |
| `sam_review_responses` | 69 | 69 | none |

**Item 10 — drift report:** none. Every count and the UID match. Preflight green.

---

## 2. Workstream B — final cutover backup + readback

A complete `service_role` logical backup of all 7 Tidy Tails public tables was
taken and independently readback-verified.

- **Tool:** `venture-ops/dump_supabase.py` v1.0.0, `--label cutover`.
- **Backup directory:** `venture-ops/backups/tidy-tails/2026-05-18-cutover/`
  (on Russell's Mac; `backups/` is gitignored — **never committed**, confirmed
  `!!` ignored in `git status`).
- **`MANIFEST.json`:** `status: complete`, `key_role: service_role`,
  `warnings: []`, `supabase_ref: pgkwovokciaqnbhpttba`,
  `observed_at: 2026-05-18T06:16:06Z` (02:16 EDT).
- **Service_role key handling:** the key was sourced from
  `venture-ops/.env.cutover` into the environment only — never printed, never
  committed. `.env.cutover` was **deleted immediately** after the backup +
  readback succeeded (confirmed gone).

**Per-file integrity anchor** (durable record — the gitignored `MANIFEST.json`
is the authoritative copy; these SHA-256s let a future operator detect tampering
even without it):

| Table | Rows | Bytes | SHA-256 |
|---|---|---|---|
| `clients` | 137 | 69848 | `3b10f4366dd5ad142b65ebcece7e44de710f0f0ea5867bb678360980e6c38237` |
| `pets` | 188 | 122618 | `65bafd6b4307234aa4813313e690d667e022205e199072afef7b179c679f4b2b` |
| `appointments` | 737 | 356603 | `e2fa534d9ff1138feb1941d6a23a974e84c561d6386c4ac0a5bd195eabf2feaa` |
| `booking_requests` | 0 | 3 | `37517e5f3dc66819f61f5a7bb8ace1921282415f10551d2defa5c3eb0985b570` |
| `client_accounts` | 0 | 3 | `37517e5f3dc66819f61f5a7bb8ace1921282415f10551d2defa5c3eb0985b570` |
| `automations_log` | 0 | 3 | `37517e5f3dc66819f61f5a7bb8ace1921282415f10551d2defa5c3eb0985b570` |
| `sam_review_responses` | 69 | 41280 | `5d1aeb89dcdefd81db5b63e5ba0afd702331bd9f685e1119a543bdcf12371b80` |

(The three empty tables share one SHA-256 — they are each an identical empty
`[]` array. Expected, not an error.)

**Readback drill — PASS.** Independently of the dump run:
- recomputed SHA-256 of every file → all 7 match `MANIFEST.json`;
- byte sizes → all 7 match;
- re-parsed every JSON → row counts match `row_count` and `server_count`;
- `clients` ids → 137 non-null, 137 unique.

**One production data observation (not a backup defect):** one `clients` row
has a blank `first_name`. The backup faithfully captured production as-is — this
is a pre-existing data quirk in Samantha's records, irrelevant to the RLS
migration, and **not** a cutover blocker. Worth a tidy-up post-cutover.

> **Freshness caveat — read before cutover day.** This backup is the cutover
> baseline **only while production has not drifted**. It was taken 2026-05-18
> 02:16 EDT. If Samantha logs more work in v1 before the cutover, the counts
> move and this `MANIFEST.json` is no longer the exact baseline. The runbook
> §2.3 (T‑15) drift recheck is what validates it: re-run the §1 item-9 count
> SELECTs immediately before apply and confirm they still equal this manifest
> (137 / 188 / 737 / 0 / 0 / 0 / 69). **If they have drifted**, take a fresh
> `service_role` backup at T‑30 — which means Russell must re-supply the
> service_role key (`.env.cutover` was deleted by design). The fresh
> `MANIFEST.json` then becomes the baseline.

---

## 3. Workstream C — cutover command package

The exact process for cutover day. **Do not run any of §3.2 until Russell types
the final-approval phrase.** §3.1 is read-only.

### 3.1 Immediately before apply (read-only — no gate)

1. Re-run the §1 item-9 count SELECTs against `pgkwovokciaqnbhpttba`; confirm
   137 / 188 / 737 / 0 / 0 / 0 / 69 (or take a fresh backup — see §2 caveat).
2. Re-run the UID query; confirm `88413167-0799-49a7-ba4c-c1c29403e038`.
3. Confirm Sam can sign in to `https://tidy-tails-v2.vercel.app` and see live
   data (runbook §2.4).

### 3.2 Apply the forward migration ⛔ REQUIRES THE FINAL-APPROVAL PHRASE

- **File:** `_reports/2026-05-18-ship-2.2b-production-forward-migration.sql`
- **Project ref:** `pgkwovokciaqnbhpttba`
- **Mechanism (one named migration):**

  ```
  apply_migration(
    project_id = 'pgkwovokciaqnbhpttba',
    name       = 'ship_2_2b_forward_rls_hardening',
    query      = <full contents of the forward-migration SQL file>
  )
  ```

  Dashboard alternative: paste the full file into the SQL Editor for
  `pgkwovokciaqnbhpttba`, wrapped in `BEGIN; … COMMIT;`. Runs in seconds; the
  file ends with `NOTIFY pgrst, 'reload schema';`.

### 3.3 Verify (read-only, immediately after apply)

Run the verification query in **runbook §2.6** against `pgkwovokciaqnbhpttba`
(the `jsonb_build_object('verify', …)` query — row counts, `groomer_id` meta +
backfill, `permissive_survivors`, `groomer_policy_count`,
`client_overview_exists`), then the anon REST checks and the
authenticated/gated-writes checks.

### 3.4 Success criteria — "cutover green" (all 8 must hold)

1. Row counts unchanged vs the cutover `MANIFEST.json` (6 in-scope tables +
   `sam_review_responses`).
2. `groomer_id` on all 6 in-scope tables; `is_nullable = NO`; default
   `auth.uid()`; every row = `88413167-0799-49a7-ba4c-c1c29403e038`, zero nulls.
3. `permissive_survivors` = `[]`; `groomer_policy_count` = `24`.
4. `client_overview_exists` = `0`.
5. Anon REST `GET` clients/pets/appointments → `200 []`; anon `POST` → `401/403`.
6. v2 authenticated reads work — Sam signs in; lists/details/reports render.
7. `get_advisors(security)` — in-scope permissive + SECURITY DEFINER lints cleared.
8. All 4 v2 write surfaces still return their **gated** result.

### 3.5 Failure criteria → rollback

- Any of criteria **1–7** fails → roll back (§3.6). Criterion **8** failing is
  an app-config fix (a write flag is open), **not** a DB rollback.
- Watch-week triggers (runbook §4.1): a blocking defect with no same-day fix;
  data integrity in doubt; any SMS Samantha did not explicitly confirm.

### 3.6 Rollback command

- **File:** `_reports/2026-05-18-ship-2.2b-production-rollback-migration.sql`

  ```
  apply_migration(
    project_id = 'pgkwovokciaqnbhpttba',
    name       = 'ship_2_2b_rollback_rls_hardening',
    query      = <full contents of the rollback-migration SQL file>
  )
  ```

  Then verify (read-only): row counts still match `MANIFEST.json` (rollback
  drops a *column*, never a *row*); `groomer_id` absent; 21 permissive policies
  + `client_overview` restored; anon REST `GET` returns rows again; advisors
  back to the 19-lint set. Second safety net if the rollback migration itself
  cannot apply: restore from the §2 service_role backup via
  `venture-ops/restore_supabase.py` (a separately-approved action).

---

## 4. Workstream D — Sam day-one readiness checklist

What "Samantha's first day on v2" looks like, and how an operator confirms it
went well. The moment the forward migration commits, **v1 goes dark and v2 is
her live tool.**

**Before she starts (operator, cutover day):**
- [ ] Cutover is green — all 8 §3.4 criteria held.
- [ ] Sam signed in at `https://tidy-tails-v2.vercel.app` as
      `sammclennan143@gmail.com` during the runbook §2.4 / §3.1 check; dashboard,
      client list, a client detail, a pet detail, and reports all rendered.
- [ ] She has been told (per the Sam launch note,
      `2026-05-18-tidy-tails-v2-sam-launch-note.md`) that v1 is retired and v2
      is the new tool.

**What is normal on day one — not a bug:**
- [ ] Reads work fully: search, client/pet detail, reports, revenue totals.
- [ ] The four action buttons — Add Appointment, Log Groom, Reminder, Add
      Household — show a "not switched on yet — nothing was saved" message.
      **This is expected.** Writes are enabled deliberately, one surface at a
      time, across the ~7-day watch week (`2026-05-18-ship-2.2b-write-gate-operations.md`).
- [ ] No SMS is sent by anything. The reminder path stays human-confirmed and
      is not wired until its own watch-week flip.

**Red flags — operator rolls back or fixes same-day (runbook §4.1):**
- [ ] Sam cannot sign in, or v2 shows no data → **stop / roll back** (do not
      leave her with no working tool).
- [ ] A client, pet, or appointment is missing, wrong, or mis-scoped.
- [ ] Any SMS goes out that Samantha did not explicitly confirm.

**First watch-week flip:** Add Appointment — only after the cutover is green and
the `lastAppointment()` future-date fix is in (precondition; see the write-gate
operations doc). Each flip is its own approval + mini-verifier.

---

## 5. The gate — what has NOT happened

This run stopped at the production SQL gate, exactly as instructed.

**Not done (and not authorized by this doc):**
- The forward migration has **not** been applied. Production schema is unchanged.
- No production data was mutated; no write flag was set; no SMS was sent.
- The cutover backup is the only thing that touched production — and it is
  strictly read-only (REST `GET` only; `dump_supabase.py` issues no writes).

**The one remaining action** is Russell's explicit final approval — typing,
exactly:

> **`FINAL APPROVAL: run the Ship 2.2b production RLS cutover now`**

A casual "yes" / "go" / "ship it" does **not** authorize the apply. On that
phrase, an operator follows §3 (and the runbook) to apply the forward migration.

---

*Generated 2026-05-18. Production `pgkwovokciaqnbhpttba` has not been mutated.
The cutover backup is taken and readback-verified; the command package is ready;
the cutover itself is gated on Russell's exact final-approval phrase.*
