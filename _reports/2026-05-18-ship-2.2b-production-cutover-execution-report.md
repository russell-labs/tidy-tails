---
title: Ship 2.2b — production cutover execution report
venture: tidy-tails
date: 2026-05-18
status: DONE — forward migration applied; verifier green on criteria 1–5 & 7
type: cutover-execution-report
ship: 2.2b
production-ref: pgkwovokciaqnbhpttba
parent-docs:
  - _reports/2026-05-18-ship-2.2b-production-cutover-runbook.md
  - _reports/2026-05-18-ship-2.2b-production-forward-migration.sql
  - _reports/2026-05-18-ship-2.2b-final-precutover-check.md
---

# Ship 2.2b — production cutover execution report

The Ship 2.2b RLS-hardening cutover was **executed on production
`pgkwovokciaqnbhpttba` on 2026-05-18**, on Russell's explicit final approval.
The forward migration is applied and committed; **v1 is dark, v2 is the live
tool** for reads. This is the durable record of what ran and what was verified.

## Sequence

1. **Russell's final-approval phrase** received — `FINAL APPROVAL: run the Ship
   2.2b production RLS cutover now`.
2. **T‑15 drift check #1 — FAILED.** `sam_review_responses` had drifted 69 → 70
   since the first backup (`2026-05-18T06:16:06Z`). Per the runbook, the cutover
   was **halted** — the backup was no longer a valid baseline. (The T‑15
   row-count recheck added during overnight prep, commit `7b2ff93`, is what
   caught this.)
3. **Fresh `service_role` backup taken** by Codex →
   `venture-ops/backups/tidy-tails/2026-05-18-cutover-151937/`,
   `observed_at 2026-05-18T15:19:37Z`, `status: complete`, `key_role:
   service_role`, `warnings: []`. Counts: 137 / 188 / 737 / 0 / 0 / 0 / **70**.
   Readback verified.
4. **T‑15 drift check #2 — CLEAN.** Production matched the fresh manifest
   exactly across all seven tables; Sam's UID confirmed; `groomer_id` absent;
   `client_overview` present — expected pre-migration state.
5. **Forward migration applied** as one named migration
   `ship_2_2b_forward_rls_hardening` (the full
   `2026-05-18-ship-2.2b-production-forward-migration.sql`), via
   `apply_migration` — returned `success: true`. Atomic: Steps A→B→C + the
   PostgREST `NOTIFY` committed together.

## Verifier results

The fresh backup `MANIFEST.json` (`2026-05-18-cutover-151937`) is the baseline.

| # | Criterion | Result |
|---|---|---|
| 1 | Row counts unchanged vs `MANIFEST` | ✅ 137 / 188 / 737 / 0 / 0 / 0 / 70 |
| 2 | `groomer_id`: NOT NULL, default `auth.uid()`, every row = Sam's UID, 0 nulls | ✅ all 6 tables — clients 137/137/0, pets 188/188/0, appointments 737/737/0, others 0/0/0 |
| 3 | `permissive_survivors`=[], `groomer_policy_count`=24 | ✅ `[]`, `24` |
| 4 | `client_overview` dropped | ✅ `client_overview_exists`=0 |
| 5 | Anon REST `GET` → `200 []`; anon `POST` rejected | ✅ clients/pets/appointments `200 []`; `POST` → `401` (`42501` RLS violation) |
| 6 | v2 authenticated reads return 137 / 188 / 737 | ⏳ **Russell's sign-in check** — see below |
| 7 | Security advisors — in-scope permissive + SECURITY DEFINER lints cleared | ✅ 19 → 6 lints; **0 ERROR**; the 6 remaining are all out-of-scope WARNs |
| 8 | All 4 v2 write surfaces still gated | ⏳ **Russell's UI check** — no `TIDYTAILS_ENABLE_*` flag is set, so they are gated by construction |

**Every criterion that can be verified by SQL/REST (1–5, 7) passed.** No hard
criterion failed, so **no rollback was needed.**

Criterion 6 is additionally guaranteed *by construction*: every row's
`groomer_id` equals Sam's UID and the SELECT policy is `groomer_id = auth.uid()`,
so an authenticated session as Samantha sees exactly all 137 / 188 / 737 rows.
The empirical sign-in confirmation is still hers/Russell's to do.

## Advisor lints — before vs after

19 security lints → **6**, all `WARN`, **no ERROR**. Cleared by the migration:
the 12 in-scope `rls_policy_always_true` lints on the 6 tables, and the
`client_overview` `security_definer_view` ERROR. The 6 remaining are all
out-of-scope and pre-existing: `sam_review_responses` anon-insert policy, the
`sam-review` storage bucket, `rls_auto_enable()` (×2), `update_updated_at_column`
search_path, and leaked-password protection.

## Note — "21 vs 20" permissive policies

The runbook/operator card said to expect "21 permissive policies." Production
had **20 in-scope** permissive policies, and the forward SQL's 20 `DROP POLICY`
statements matched them **1:1** by name — no survivor. The "21" counted 20
in-scope **+ 1 out-of-scope** (`sam_review_responses`'s `sam_review_anon_insert`,
correctly left untouched). The forward SQL was correct; only that doc figure was
imprecise.

## State after cutover

- **R‑1 closed** — the public anon key can no longer read or write the 6
  in-scope tables. v1 (static HTML + anon key) is dark.
- **v2 is live for reads.** All four write surfaces remain **gated** — no
  `TIDYTAILS_ENABLE_*` flag is set. The migration made writes *safe*, not
  *active*.
- Production row data is unchanged — the migration added a column and backfilled
  it; it created/deleted no rows.

## Remaining

1. **Russell:** sign in to v2 as `sammclennan143@gmail.com` and confirm
   criterion 6 (client list / detail / pet / reports render) and criterion 8
   (the 4 write surfaces still show their gated message). Criterion 8 failing
   would be an app-config fix, not a DB rollback.
2. **Watch week** — per the runbook §4.1 rollback triggers.
3. **Write flips** — Add Appointment first, then Log Groom, one at a time, each
   its own approval — see `2026-05-18-ship-2.2b-flips-1-2-operator-cards.md`.

---

*Generated 2026-05-18. The Ship 2.2b forward migration is applied and committed
to production `pgkwovokciaqnbhpttba`. Verifier criteria 1–5 and 7 passed; 6 and
8 await Russell's sign-in check. No rollback was required.*
